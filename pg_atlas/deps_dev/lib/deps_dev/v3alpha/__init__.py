# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: api-v3alpha.proto
# plugin: python-betterproto2
# This file has been @generated

__all__ = (
    "Advisory",
    "AdvisoryKey",
    "Attestation",
    "Capabilities",
    "CapabilitiesCapability",
    "Dependencies",
    "DependenciesEdge",
    "DependenciesNode",
    "DependencyRelation",
    "Dependents",
    "DockerHubRepoMetadata",
    "Finding",
    "FindingCooldown",
    "FindingDeprecated",
    "FindingLowUsage",
    "FindingRisk",
    "FindingType",
    "Findings",
    "FindingsBatch",
    "FindingsBatchResponse",
    "FindingsVersion",
    "GetAdvisoryRequest",
    "GetCapabilitiesRequest",
    "GetDependenciesRequest",
    "GetDependentsRequest",
    "GetFindingsBatchRequest",
    "GetFindingsRequest",
    "GetPackageRequest",
    "GetProjectBatchRequest",
    "GetProjectPackageVersionsRequest",
    "GetProjectRequest",
    "GetRequirementsRequest",
    "GetSimilarlyNamedPackagesRequest",
    "GetVersionBatchRequest",
    "GetVersionRequest",
    "Hash",
    "HashType",
    "InsightsStub",
    "Link",
    "Package",
    "PackageKey",
    "PackageVersion",
    "Project",
    "ProjectBatch",
    "ProjectBatchResponse",
    "ProjectKey",
    "ProjectOssFuzzDetails",
    "ProjectPackageVersions",
    "ProjectPackageVersionsVersion",
    "ProjectRelationProvenance",
    "ProjectRelationType",
    "ProjectScorecard",
    "ProjectScorecardCheck",
    "ProjectScorecardCheckDocumentation",
    "ProjectScorecardRepository",
    "ProjectScorecardScorecardDetails",
    "PurlLookupBatchRequest",
    "PurlLookupBatchResult",
    "PurlLookupBatchResultResponse",
    "PurlLookupRequest",
    "PurlLookupResult",
    "QueryContainerImagesRequest",
    "QueryContainerImagesResult",
    "QueryContainerImagesResultResult",
    "QueryRequest",
    "QueryResult",
    "QueryResultResult",
    "QueryResultResultArtifact",
    "Requirements",
    "RequirementsCargo",
    "RequirementsCargoDependency",
    "RequirementsCargoFeature",
    "RequirementsGo",
    "RequirementsGoDependency",
    "RequirementsGoReplace",
    "RequirementsMaven",
    "RequirementsMavenDependency",
    "RequirementsMavenProfile",
    "RequirementsMavenProfileActivation",
    "RequirementsMavenProfileActivationFile",
    "RequirementsMavenProfileActivationJdk",
    "RequirementsMavenProfileActivationOs",
    "RequirementsMavenProfileActivationProperty",
    "RequirementsMavenProperty",
    "RequirementsMavenRepository",
    "RequirementsNpm",
    "RequirementsNpmBundle",
    "RequirementsNpmDependencies",
    "RequirementsNpmDependenciesDependency",
    "RequirementsNpmDependenciesPeerDependencyMetadata",
    "RequirementsNuGet",
    "RequirementsNuGetDependencyGroup",
    "RequirementsNuGetDependencyGroupDependency",
    "RequirementsNuGetFrameworkAssembly",
    "RequirementsNuGetFrameworkReference",
    "RequirementsPyPi",
    "RequirementsPyPiDependency",
    "RequirementsPyPiExternalDependency",
    "RequirementsPyPiExtra",
    "RequirementsRubyGems",
    "RequirementsRubyGemsDependency",
    "SimilarlyNamedPackages",
    "SimilarlyNamedPackagesPackage",
    "SlsaProvenance",
    "System",
    "UpstreamIdentifier",
    "Version",
    "VersionBatch",
    "VersionBatchResponse",
    "VersionCooldown",
    "VersionKey",
    "VersionLicense",
    "VersionProject",
    "VersionProjectStatus",
)

import datetime
from dataclasses import dataclass
from typing import TYPE_CHECKING

import betterproto2
from betterproto2 import grpclib as betterproto2_grpclib

from ...message_pool import default_message_pool

if TYPE_CHECKING:
    from betterproto2.grpclib.grpclib_client import MetadataLike
    from grpclib.metadata import Deadline

_COMPILER_VERSION = "0.10.1"
betterproto2.check_compiler_version(_COMPILER_VERSION)


class DependencyRelation(betterproto2.Enum):
    """
    DependencyRelation describes the relation of a node within a dependency
    graph.
    """

    DEPENDENCY_RELATION_UNSPECIFIED = 0

    SELF = 1

    DIRECT = 2

    INDIRECT = 3


class FindingRisk(betterproto2.Enum):
    UNSPECIFIED = 0

    CRITICAL = 1
    """
    This finding represents a critical security risk, and should be
    immediately actioned.
    """

    HIGH = 2
    """
    This finding represents a high security risk, and the relevant
    package or version should be avoided if possible.
    """

    MEDIUM = 3
    """
    This finding represents a potential security risk.
    """

    LOW = 4
    """
    Currently unused but reserved for future use.
    """

    INFORMATIONAL = 5
    """
    This finding does not inherently convey a security risk and is
    informational only. Recommended treatment varies based on the type
    of the finding.
    """

    @classmethod
    def betterproto_value_to_renamed_proto_names(cls) -> dict[int, str]:
        return {
            0: "RISK_UNSPECIFIED",
            1: "RISK_CRITICAL",
            2: "RISK_HIGH",
            3: "RISK_MEDIUM",
            4: "RISK_LOW",
            5: "RISK_INFORMATIONAL",
        }

    @classmethod
    def betterproto_renamed_proto_names_to_value(cls) -> dict[str, int]:
        return {
            "RISK_UNSPECIFIED": 0,
            "RISK_CRITICAL": 1,
            "RISK_HIGH": 2,
            "RISK_MEDIUM": 3,
            "RISK_LOW": 4,
            "RISK_INFORMATIONAL": 5,
        }


class FindingType(betterproto2.Enum):
    TYPE_UNSPECIFIED = 0

    NOT_FOUND = 1
    """
    The package or version was not found. If the package is not found within
    the specified ecosystem, or all versions have been deleted, this will be
    a package-scoped finding. If the specified version was not found but the
    package was, this will be a version-scoped finding.
    """

    MALICIOUS = 2
    """
    The package or version has a malicious advisory.
    """

    DEPRECATED = 3
    """
    The package or version has been marked as deprecated.
    """

    COOLDOWN = 4
    """
    The package or version is in cooldown. This is a security practice that
    mitigates supply chain attacks by delaying the update of newly published
    package versions.
    """

    LOW_USAGE = 5
    """
    The package has low usage.
    """

    VULNERABLE = 6
    """
    This package or version is affected by critical vulnerabilities.
    """

    REMEDIATION = 7
    """
    This version remediates a security vulnerability.
    """


class HashType(betterproto2.Enum):
    """
    HashType identifies a function used to produce a hash.
    """

    HASH_TYPE_UNSPECIFIED = 0

    MD5 = 1

    SHA1 = 2

    SHA256 = 3

    SHA512 = 4


class ProjectRelationProvenance(betterproto2.Enum):
    """
    How the mapping between project and package version was discovered.
    """

    UNKNOWN_PROJECT_RELATION_PROVENANCE = 0

    SLSA_ATTESTATION = 1
    """
    There is a SLSA attestation that links this package version to this
    project.
    """

    GO_ORIGIN = 2
    """
    This project contains the package version (Go specific).
    """

    PYPI_PUBLISH_ATTESTATION = 3
    """
    There is a [PyPI Publish
    attestation](https://docs.pypi.org/attestations/publish/v1/) that links
    this package version to this project.
    """

    RUBYGEMS_PUBLISH_ATTESTATION = 4
    """
    There is a RubyGems Publish attestation that links this package version to
    this project.
    """

    UNVERIFIED_METADATA = 10
    """
    The package version's metadata contains an unverified link to this project.
    """


class ProjectRelationType(betterproto2.Enum):
    """
    ProjectRelationType specifies a relationship between a project and a package
    version.
    """

    UNKNOWN_PROJECT_RELATION_TYPE = 0

    SOURCE_REPO = 1
    """
    This project is this package version's source code repository.
    """

    ISSUE_TRACKER = 2
    """
    This project is the package version's issue tracker.
    """


class System(betterproto2.Enum):
    """
    System identifies a package management system.
    """

    SYSTEM_UNSPECIFIED = 0

    GO = 1

    RUBYGEMS = 2

    NPM = 3

    CARGO = 4

    MAVEN = 6

    PYPI = 7

    NUGET = 8


@dataclass(eq=False, repr=False)
class Advisory(betterproto2.Message):
    """
    Advisory holds information about a security advisory hosted by OSV.
    """

    advisory_key: "AdvisoryKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The identifier for the security advisory. Note that this may differ from
    the identifier in the request, due to canonicalization.
    """

    url: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The URL of the security advisory.
    """

    title: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    A brief human-readable description.
    """

    aliases: "list[str]" = betterproto2.field(4, betterproto2.TYPE_STRING, repeated=True)
    """
    Other identifiers used for the advisory, including CVEs.
    """

    cvss3_score: "float" = betterproto2.field(5, betterproto2.TYPE_FLOAT)
    """
    The severity of the advisory as a CVSS v3 score in the range [0,10].
    A higher score represents greater severity.
    """

    cvss3_vector: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    The severity of the advisory as a CVSS v3 vector string.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Advisory", Advisory)


@dataclass(eq=False, repr=False)
class AdvisoryKey(betterproto2.Message):
    """
    AdvisoryKey identifies a security advisory.
    """

    id: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The OSV identifier for the security advisory.
    """


default_message_pool.register_message("deps_dev.v3alpha", "AdvisoryKey", AdvisoryKey)


@dataclass(eq=False, repr=False)
class Attestation(betterproto2.Message):
    """
    Attestation represents a generic attestation. Fields are populated based
    on 'type'.
    """

    type: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The type of attestation.
    One of https://slsa.dev/provenance/v0.2, https://slsa.dev/provenance/v1,
    https://docs.pypi.org/attestations/publish/v1.
    """

    url: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The URL of the attestation if there is one.
    """

    verified: "bool" = betterproto2.field(3, betterproto2.TYPE_BOOL)
    """
    The attestation has been cryptographically verified by deps.dev.
    For attestations distributed in a Sigstore bundle, this field indicates
    the bundle was verified using the
    [sigstore-go](https://github.com/sigstore/sigstore-go) library.
    """

    source_repository: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    Only set if type is https://slsa.dev/provenance/v0.2,
    https://slsa.dev/provenance/v1,
    https://docs.pypi.org/attestations/publish/v1.
    The source code repository used to build the version.
    """

    commit: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    The commit of the source code repository the version was built from.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Attestation", Attestation)


@dataclass(eq=False, repr=False)
class Capabilities(betterproto2.Message):
    """
    Capabilities holds identified capabilities and callpaths for a package
    version.
    """

    capabilities: "list[CapabilitiesCapability]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The Capslock capabilities associated with a package, along with the number
    of direct and indirect callpaths to this capability.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Capabilities", Capabilities)


@dataclass(eq=False, repr=False)
class CapabilitiesCapability(betterproto2.Message):
    """
    Capabilities stores the capabilities identified for the package.
    """

    capability: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    A Capslock capability, indicating that the packages uses this capability.
    """

    direct_count: "int" = betterproto2.field(2, betterproto2.TYPE_UINT32)
    """
    The number of calls from this package directly to this capability.
    """

    indirect_count: "int" = betterproto2.field(3, betterproto2.TYPE_UINT32)
    """
    The number of calls from this package to the capability via another
    package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Capabilities.Capability", CapabilitiesCapability)


@dataclass(eq=False, repr=False)
class Dependencies(betterproto2.Message):
    """
    Dependencies holds a resolved dependency graph for a package version.

    The dependency graph should be similar to one produced by installing the
    package version on a generic 64-bit Linux system, with no other dependencies
    present. The precise meaning of this varies from system to system.
    """

    nodes: "list[DependenciesNode]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The nodes of the dependency graph. The first node is the root of the graph.
    """

    edges: "list[DependenciesEdge]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The edges of the dependency graph.
    """

    error: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Any error associated with the dependency graph that is not specific to a
    node. An error here may imply the graph as a whole is incorrect.
    This error message has no defined format and is intended for human
    consumption.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Dependencies", Dependencies)


@dataclass(eq=False, repr=False)
class DependenciesEdge(betterproto2.Message):
    """
    Edge represents a directed edge in a resolved dependency graph: a
    dependency relation between two nodes.
    """

    from_node: "int" = betterproto2.field(1, betterproto2.TYPE_UINT32)
    """
    The node declaring the dependency, specified as an index into the list of
    nodes.
    """

    to_node: "int" = betterproto2.field(2, betterproto2.TYPE_UINT32)
    """
    The node resolving the dependency, specified as an index into the list of
    nodes.
    """

    requirement: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The requirement resolved by this edge, as declared by the "from" node.
    The meaning of this field is system-specific. As an example, in npm, the
    requirement "^1.0.0" may be resolved by the version "1.2.3".
    """


default_message_pool.register_message("deps_dev.v3alpha", "Dependencies.Edge", DependenciesEdge)


@dataclass(eq=False, repr=False)
class DependenciesNode(betterproto2.Message):
    """
    Node represents a node in a resolved dependency graph.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The package version represented by this node. Note that the package and
    version name may differ from the names in the request, if provided, due
    to canonicalization.

    In some systems, a graph may contain multiple nodes for the same package
    version.
    """

    bundled: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    If true, this is a bundled dependency.

    For bundled dependencies, the package name in the version key encodes
    how the dependency is bundled. As an example, a bundled dependency with
    a name like "a>1.2.3>b>c" is part of the dependency graph of package "a"
    at version "1.2.3", and has the local name "c". It may or may not be the
    same as a package with the global name "c".
    """

    relation: "DependencyRelation" = betterproto2.field(
        4, betterproto2.TYPE_ENUM, default_factory=lambda: DependencyRelation(0)
    )
    """
    Whether this node represents a direct or indirect dependency within this
    dependency graph. Note that it's possible for a dependency to be both
    direct and indirect; if so, it is marked as direct.
    """

    errors: "list[str]" = betterproto2.field(3, betterproto2.TYPE_STRING, repeated=True)
    """
    Errors associated with this node of the graph, such as an unresolved
    dependency requirement. An error on a node may imply the graph as a
    whole is incorrect. These error messages have no defined format and are
    intended for human consumption.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Dependencies.Node", DependenciesNode)


@dataclass(eq=False, repr=False)
class Dependents(betterproto2.Message):
    """
    Dependents holds information about the number of distinct packages known to
    depend on a package version.

    Dependent counts are derived from the dependency graphs computed by
    deps.dev, which means that only public dependents are counted. As such,
    dependent counts should be treated as indicative of relative popularity
    rather than precisely accurate.
    """

    dependent_count: "int" = betterproto2.field(1, betterproto2.TYPE_UINT32)
    """
    The number of packages known to depend on this package version, either
    directly or indirectly. Note that this may be less than the sum of the
    direct and indirect dependent counts.
    """

    direct_dependent_count: "int" = betterproto2.field(2, betterproto2.TYPE_UINT32)
    """
    The number of packages known to depend directly on this package version.
    """

    indirect_dependent_count: "int" = betterproto2.field(3, betterproto2.TYPE_UINT32)
    """
    The number of packages known to depend indirectly on this package version.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Dependents", Dependents)


@dataclass(eq=False, repr=False)
class DockerHubRepoMetadata(betterproto2.Message):
    date_registered: "datetime.datetime | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The date the image's repository was registered.
    """

    last_updated: "datetime.datetime | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The date the image's repository was last updated.
    """

    star_count: "int" = betterproto2.field(3, betterproto2.TYPE_INT64)
    """
    The number of stars.
    """

    pull_count: "int" = betterproto2.field(4, betterproto2.TYPE_INT64)
    """
    The number of times an image has been pulled.
    """

    is_official: "bool" = betterproto2.field(5, betterproto2.TYPE_BOOL)
    """
    Specifies if the image's repository is official, based on the registry.
    An image is considered official if it is present in
    https://github.com/docker-library/official-images.
    """


default_message_pool.register_message("deps_dev.v3alpha", "DockerHubRepoMetadata", DockerHubRepoMetadata)


@dataclass(eq=False, repr=False)
class Finding(betterproto2.Message):
    """
    Finding contains a single actionable finding.

    Oneofs:
        - context:
    """

    type: "FindingType" = betterproto2.field(1, betterproto2.TYPE_ENUM, default_factory=lambda: FindingType(0))
    """
    The type of finding indicates what specific thing was found.
    """

    risk: "FindingRisk" = betterproto2.field(2, betterproto2.TYPE_ENUM, default_factory=lambda: FindingRisk(0))
    """
    The risk level of the finding indicates how urgent it is to action.
    """

    deprecated_context: "FindingDeprecated | None" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, optional=True, group="context"
    )

    cooldown_context: "FindingCooldown | None" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, optional=True, group="context"
    )

    low_usage_context: "FindingLowUsage | None" = betterproto2.field(
        5, betterproto2.TYPE_MESSAGE, optional=True, group="context"
    )


default_message_pool.register_message("deps_dev.v3alpha", "Finding", Finding)


@dataclass(eq=False, repr=False)
class FindingCooldown(betterproto2.Message):
    end: "datetime.datetime | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The point in time until which this version is in cooldown.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Finding.Cooldown", FindingCooldown)


@dataclass(eq=False, repr=False)
class FindingDeprecated(betterproto2.Message):
    reason: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    A human-readable description of why the package/version was deprecated.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Finding.Deprecated", FindingDeprecated)


@dataclass(eq=False, repr=False)
class FindingLowUsage(betterproto2.Message):
    alternative_packages: "list[str]" = betterproto2.field(1, betterproto2.TYPE_STRING, repeated=True)
    """
    Packages with similar names that have higher usage.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Finding.LowUsage", FindingLowUsage)


@dataclass(eq=False, repr=False)
class Findings(betterproto2.Message):
    """
    Findings contains package and version findings which are relevant to
    safe dependency management.

    Oneofs:
        - key: The package or version which was requested. The package and version name
            may differ from what was specified in the request, due to canonicalization.
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True, group="key")

    version_key: "VersionKey | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True, group="key")

    recommended_versions: "list[FindingsVersion]" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    A set of low risk versions recommended for use. May be empty.
    """

    requested_version: "FindingsVersion | None" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, optional=True)
    """
    If a specific version was requested, findings that apply to that version.
    May be empty.
    """

    default_version: "FindingsVersion | None" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The version that is installed when no version is specified. The precise
    meaning of this is system-specific, but it is commonly the version with
    the greatest version number, ignoring pre-release versions.
    """

    package_findings: "list[Finding]" = betterproto2.field(6, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Findings which are package-scoped and apply to every version of this
    package. For example, if there is a MALICIOUS package finding, all
    versions in the package are malicious.
    Package-scoped findings are usually also included within the
    findings for each version.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Findings", Findings)


@dataclass(eq=False, repr=False)
class FindingsVersion(betterproto2.Message):
    """
    Findings for a particular package version.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The version these findings apply to. The package and version name may
    differ from what was specified in the request, due to canonicalization.
    """

    is_default: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    If true, this is the default version of the package: the version that is
    installed when no version is specified. The precise meaning of this is
    system-specific, but it is commonly the version with the greatest
    version number, ignoring pre-release versions.
    """

    findings: "list[Finding]" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    All findings associated with this package version, including all
    findings in package_findings.
    """

    cooldown_end: "datetime.datetime | None" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The point in time until which this version is in cooldown.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Findings.Version", FindingsVersion)


@dataclass(eq=False, repr=False)
class FindingsBatch(betterproto2.Message):
    """
    FindingsBatch contains a batch of findings information. If there is more
    findings information to be returned, a page token is included in the
    response.
    """

    responses: "list[FindingsBatchResponse]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The findings for this page.
    """

    next_page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, this batch is not the full result set. This page token
    may be used to fetch more results in a subsequent request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "FindingsBatch", FindingsBatch)


@dataclass(eq=False, repr=False)
class FindingsBatchResponse(betterproto2.Message):
    request: "GetFindingsRequest | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The uncanonicalized request.
    """

    findings: "Findings | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The findings for the requested package/version.
    """


default_message_pool.register_message("deps_dev.v3alpha", "FindingsBatch.Response", FindingsBatchResponse)


@dataclass(eq=False, repr=False)
class GetAdvisoryRequest(betterproto2.Message):
    """
    GetAdvisoryRequest identifies a security advisory for which to return
    information.
    """

    advisory_key: "AdvisoryKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetAdvisoryRequest", GetAdvisoryRequest)


@dataclass(eq=False, repr=False)
class GetCapabilitiesRequest(betterproto2.Message):
    """
    GetCapabilitiesRequest identifies a package version for which to return
    capabilities.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetCapabilitiesRequest", GetCapabilitiesRequest)


@dataclass(eq=False, repr=False)
class GetDependenciesRequest(betterproto2.Message):
    """
    GetDependenciesRequest identifies a package version for which to return
    dependencies.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetDependenciesRequest", GetDependenciesRequest)


@dataclass(eq=False, repr=False)
class GetDependentsRequest(betterproto2.Message):
    """
    GetDependentsRequest identifies a package version for which to return
    dependent counts.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetDependentsRequest", GetDependentsRequest)


@dataclass(eq=False, repr=False)
class GetFindingsBatchRequest(betterproto2.Message):
    """
    GetFindingsBatchRequest identifies a batch of packages/versions for which to
    return findings.
    """

    requests: "list[GetFindingsRequest]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The batch list of packages/versions to return findings for.
    Up to 5000 requests are allowed in a single batch.
    """

    page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, request the next page of the result set. It must be set to the
    page token provided by the previous findings batch response. All other
    request fields must be the same as in the initial request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "GetFindingsBatchRequest", GetFindingsBatchRequest)


@dataclass(eq=False, repr=False)
class GetFindingsRequest(betterproto2.Message):
    """
    GetFindings identifies a package to get findings for.

    Oneofs:
        - key:
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True, group="key")

    version_key: "VersionKey | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True, group="key")


default_message_pool.register_message("deps_dev.v3alpha", "GetFindingsRequest", GetFindingsRequest)


@dataclass(eq=False, repr=False)
class GetPackageRequest(betterproto2.Message):
    """
    GetPackageRequest identifies a package for which to return information.
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetPackageRequest", GetPackageRequest)


@dataclass(eq=False, repr=False)
class GetProjectBatchRequest(betterproto2.Message):
    """
    GetProjectBatchRequest identifies a batch of projects for which to return
    Project information.
    """

    requests: "list[GetProjectRequest]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The batch list of projects to return Project information for.
    Up to 5000 requests are allowed in a single batch.
    """

    page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, request the next page of the result set. It must be set to the
    page token provided by the previous project batch response. All other
    request fields must be the same as in the initial request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "GetProjectBatchRequest", GetProjectBatchRequest)


@dataclass(eq=False, repr=False)
class GetProjectPackageVersionsRequest(betterproto2.Message):
    project_key: "ProjectKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetProjectPackageVersionsRequest", GetProjectPackageVersionsRequest)


@dataclass(eq=False, repr=False)
class GetProjectRequest(betterproto2.Message):
    """
    GetProjectRequest identifies a project for which to return information.
    """

    project_key: "ProjectKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetProjectRequest", GetProjectRequest)


@dataclass(eq=False, repr=False)
class GetRequirementsRequest(betterproto2.Message):
    """
    GetRequirementsRequest identifies a version for which to return
    requirements.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetRequirementsRequest", GetRequirementsRequest)


@dataclass(eq=False, repr=False)
class GetSimilarlyNamedPackagesRequest(betterproto2.Message):
    """
    GetSimilarlyNamedPackagesRequest identifies a package for which to return
    similarly-named packages.
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetSimilarlyNamedPackagesRequest", GetSimilarlyNamedPackagesRequest)


@dataclass(eq=False, repr=False)
class GetVersionBatchRequest(betterproto2.Message):
    """
    GetVersionBatchRequest identifies a batch of versions for which to return
    Version information.
    """

    requests: "list[GetVersionRequest]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The batch list of versions to return Version information for.
    Up to 5000 requests are allowed in a single batch.
    """

    page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, request the next page of the result set. It must be set to the
    page token provided by the previous version batch response. All other
    request fields must be the same as in the initial request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "GetVersionBatchRequest", GetVersionBatchRequest)


@dataclass(eq=False, repr=False)
class GetVersionRequest(betterproto2.Message):
    """
    GetVersionRequest identifies a package version for which to return
    information.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)


default_message_pool.register_message("deps_dev.v3alpha", "GetVersionRequest", GetVersionRequest)


@dataclass(eq=False, repr=False)
class Hash(betterproto2.Message):
    """
    Hash represents the output of a hash function. These messages are used to
    identify package version artifacts by content hash.
    """

    type: "HashType" = betterproto2.field(1, betterproto2.TYPE_ENUM, default_factory=lambda: HashType(0))
    """
    The function used to produce this hash.
    """

    value: "bytes" = betterproto2.field(2, betterproto2.TYPE_BYTES)
    """
    A hash value.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Hash", Hash)


@dataclass(eq=False, repr=False)
class Link(betterproto2.Message):
    """
    Link represents a link declared by or derived from package version metadata,
    to an external web resource such as a homepage or source code repository.
    """

    label: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    A label describing the resource that the link points to.
    """

    url: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The URL of the link.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Link", Link)


@dataclass(eq=False, repr=False)
class Package(betterproto2.Message):
    """
    Package holds information about a package, including a list of its available
    versions, with the default version marked if known.
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The name of the package. Note that it may differ from the name in the
    request, due to canonicalization.
    """

    purl: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The purl that identifies this package. Note that the package name
    may differ from the name in the request, due to canonicalization.
    """

    versions: "list[PackageVersion]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The available versions of the package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Package", Package)


@dataclass(eq=False, repr=False)
class PackageVersion(betterproto2.Message):
    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The name of the version. Note that the package name may differ from the
    name in the request, due to canonicalization.
    """

    purl: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The purl that identifies this version of the package.
    Note that the package and version name in the purl may differ from the
    names in the request, due to canonicalization.
    """

    published_at: "datetime.datetime | None" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The time when this package version was published, if available, as
    reported by the package management authority.
    """

    is_default: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    If true, this is the default version of the package: the version that is
    installed when no version is specified. The precise meaning of this is
    system-specific, but it is commonly the version with the greatest
    version number, ignoring pre-release versions.
    """

    is_deprecated: "bool" = betterproto2.field(5, betterproto2.TYPE_BOOL)
    """
    If true, this version has been marked as deprecated.
    """

    deprecated_reason: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    The reason why this version is deprecated.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Package.Version", PackageVersion)


@dataclass(eq=False, repr=False)
class PackageKey(betterproto2.Message):
    """
    PackageKey identifies a package by name.
    """

    system: "System" = betterproto2.field(1, betterproto2.TYPE_ENUM, default_factory=lambda: System(0))
    """
    The package management system containing the package.
    """

    name: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The name of the package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PackageKey", PackageKey)


@dataclass(eq=False, repr=False)
class Project(betterproto2.Message):
    """
    Project holds information about a project hosted by GitHub, GitLab, or
    Bitbucket.
    """

    project_key: "ProjectKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The identifier for the project. Note that this may differ from the
    identifier in the request, due to canonicalization.
    """

    open_issues_count: "int" = betterproto2.field(2, betterproto2.TYPE_INT32)
    """
    The number of open issues reported by the project host.
    Only available for GitHub and GitLab.
    """

    stars_count: "int" = betterproto2.field(3, betterproto2.TYPE_INT32)
    """
    The number of stars reported by the project host.
    Only available for GitHub and GitLab.
    """

    forks_count: "int" = betterproto2.field(4, betterproto2.TYPE_INT32)
    """
    The number of forks reported by the project host.
    Only available for GitHub and GitLab.
    """

    license: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    The license reported by the project host.
    """

    description: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    The description reported by the project host.
    """

    homepage: "str" = betterproto2.field(7, betterproto2.TYPE_STRING)
    """
    The homepage reported by the project host.
    """

    scorecard: "ProjectScorecard | None" = betterproto2.field(8, betterproto2.TYPE_MESSAGE, optional=True)
    """
    An [OpenSSF Scorecard](https://github.com/ossf/scorecard) for the project,
    if one is available.
    """

    oss_fuzz: "ProjectOssFuzzDetails | None" = betterproto2.field(9, betterproto2.TYPE_MESSAGE, optional=True)
    """
    Details of this project's testing by the
    [OSS-Fuzz service](https://google.github.io/oss-fuzz/).
    Only set if the project is tested by OSS-Fuzz.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Project", Project)


@dataclass(eq=False, repr=False)
class ProjectOssFuzzDetails(betterproto2.Message):
    line_count: "int" = betterproto2.field(1, betterproto2.TYPE_INT32)
    """
    The total number of lines of code in the project.
    """

    line_cover_count: "int" = betterproto2.field(2, betterproto2.TYPE_INT32)
    """
    The number of lines of code covered by fuzzing.
    """

    date: "datetime.datetime | None" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The date the fuzz test that produced the coverage information was run
    against this project.
    The time portion of this field is midnight UTC.
    """

    config_url: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The URL containing the configuration for the project in the
    OSS-Fuzz repository.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Project.OSSFuzzDetails", ProjectOssFuzzDetails)


@dataclass(eq=False, repr=False)
class ProjectScorecard(betterproto2.Message):
    date: "datetime.datetime | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The date at which the scorecard was produced.
    The time portion of this field is midnight UTC.
    """

    repository: "ProjectScorecardRepository | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The source code repository and commit the scorecard was produced from.
    """

    scorecard: "ProjectScorecardScorecardDetails | None" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The version and commit of the Scorecard program used to produce the
    scorecard.
    """

    checks: "list[ProjectScorecardCheck]" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The results of the
    [Scorecard Checks](https://github.com/ossf/scorecard#scorecard-checks)
    performed on the project.
    """

    overall_score: "float" = betterproto2.field(5, betterproto2.TYPE_FLOAT)
    """
    A weighted average score in the range [0,10]. A higher score is better.
    """

    metadata: "list[str]" = betterproto2.field(6, betterproto2.TYPE_STRING, repeated=True)
    """
    Additional metadata associated with the scorecard.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Project.Scorecard", ProjectScorecard)


@dataclass(eq=False, repr=False)
class ProjectScorecardCheck(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the check.
    """

    documentation: "ProjectScorecardCheckDocumentation | None" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    Human-readable documentation for the check.
    """

    score: "int" = betterproto2.field(3, betterproto2.TYPE_INT32)
    """
    A score in the range [0,10]. A higher score is better.
    A negative score indicates that the check did not run successfully.
    """

    reason: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The reason for the score.
    """

    details: "list[str]" = betterproto2.field(5, betterproto2.TYPE_STRING, repeated=True)
    """
    Further details regarding the check.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Project.Scorecard.Check", ProjectScorecardCheck)


@dataclass(eq=False, repr=False)
class ProjectScorecardCheckDocumentation(betterproto2.Message):
    short_description: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    A short description of the check.
    """

    url: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    A link to more details about the check.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Project.Scorecard.Check.Documentation", ProjectScorecardCheckDocumentation
)


@dataclass(eq=False, repr=False)
class ProjectScorecardRepository(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The source code repository the scorecard was produced from.
    """

    commit: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The source code commit the scorecard was produced from.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Project.Scorecard.Repository", ProjectScorecardRepository)


@dataclass(eq=False, repr=False)
class ProjectScorecardScorecardDetails(betterproto2.Message):
    version: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The version of the Scorecard program used to produce the scorecard.
    """

    commit: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The commit of the Scorecard program used to produce the scorecard.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Project.Scorecard.ScorecardDetails", ProjectScorecardScorecardDetails
)


@dataclass(eq=False, repr=False)
class ProjectBatch(betterproto2.Message):
    """
    ProjectBatch contains a batch of project information. If there is more
    project information to be returned, a page token is included in the
    response.
    """

    responses: "list[ProjectBatchResponse]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The Project information for this page.
    """

    next_page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, this batch is not the full result set. This page token
    may be used to fetch more results in a subsequent request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "ProjectBatch", ProjectBatch)


@dataclass(eq=False, repr=False)
class ProjectBatchResponse(betterproto2.Message):
    request: "GetProjectRequest | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The uncanonicalized request.
    """

    project: "Project | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The project information for the request. If the project was not found,
    this field is empty.
    """


default_message_pool.register_message("deps_dev.v3alpha", "ProjectBatch.Response", ProjectBatchResponse)


@dataclass(eq=False, repr=False)
class ProjectKey(betterproto2.Message):
    """
    ProjectKey identifies a project.
    """

    id: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    A project identifier of the form `github.com/user/repo`,
    `gitlab.com/user/repo`, or `bitbucket.org/user/repo`.
    """


default_message_pool.register_message("deps_dev.v3alpha", "ProjectKey", ProjectKey)


@dataclass(eq=False, repr=False)
class ProjectPackageVersions(betterproto2.Message):
    versions: "list[ProjectPackageVersionsVersion]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The versions that were built from the source code contained in this
    project.
    """


default_message_pool.register_message("deps_dev.v3alpha", "ProjectPackageVersions", ProjectPackageVersions)


@dataclass(eq=False, repr=False)
class ProjectPackageVersionsVersion(betterproto2.Message):
    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The identifier for the version.
    """

    relation_type: "ProjectRelationType" = betterproto2.field(
        3, betterproto2.TYPE_ENUM, default_factory=lambda: ProjectRelationType(0)
    )
    """
    What the relationship between the project and the package version is.
    """

    relation_provenance: "ProjectRelationProvenance" = betterproto2.field(
        4, betterproto2.TYPE_ENUM, default_factory=lambda: ProjectRelationProvenance(0)
    )
    """
    How the mapping between project and package version was discovered.
    """

    slsa_provenances: "list[SlsaProvenance]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The SLSA provenance statements that link the version to the project. This
    is only populated for npm package versions. See the 'attestations' field
    for more attestations (including SLSA provenance) for all systems.
    """

    attestations: "list[Attestation]" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Attestations that link the version to the project.
    """


default_message_pool.register_message("deps_dev.v3alpha", "ProjectPackageVersions.Version", ProjectPackageVersionsVersion)


@dataclass(eq=False, repr=False)
class PurlLookupBatchRequest(betterproto2.Message):
    """
    PurlLookupBatchRequest identifies a batch of purls to search for.
    """

    requests: "list[PurlLookupRequest]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The batch list of purls to search for.
    Up to 5000 requests are allowed in a single batch.
    """

    page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, request the next page of the result set. It must be set to the
    page token provided by the previous purl lookup batch response. All other
    request fields must be the same as in the initial request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PurlLookupBatchRequest", PurlLookupBatchRequest)


@dataclass(eq=False, repr=False)
class PurlLookupBatchResult(betterproto2.Message):
    """
    PurlLookupBatchResult contains a batch of PurlLookupResults. If there is more
    information to be returned, a page token is included in the response.
    """

    responses: "list[PurlLookupBatchResultResponse]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The purl lookup results for this page.
    """

    next_page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, this batch is not the full result set. This page token
    may be used to fetch more results in a subsequent request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PurlLookupBatchResult", PurlLookupBatchResult)


@dataclass(eq=False, repr=False)
class PurlLookupBatchResultResponse(betterproto2.Message):
    request: "PurlLookupRequest | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The uncanonicalized request.
    """

    result: "PurlLookupResult | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The result corresponding to the request. If no result was found for the
    request, this field is empty.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PurlLookupBatchResult.Response", PurlLookupBatchResultResponse)


@dataclass(eq=False, repr=False)
class PurlLookupRequest(betterproto2.Message):
    """
    PurlLookupRequest identifies a purl to search for.
    """

    purl: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The purl to search for.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PurlLookupRequest", PurlLookupRequest)


@dataclass(eq=False, repr=False)
class PurlLookupResult(betterproto2.Message):
    """
    PurlLookupResult holds information corresponding to the queried purl,
    which may be either package, or package version.
    Only one field will be populated, which depends on the form of the queried
    purl.
    """

    package: "Package | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    Package result (as from GetPackage) for purls that do not include a
    version.
    """

    version: "Version | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    Version result (as from GetVersion), for purls that include a version.
    """


default_message_pool.register_message("deps_dev.v3alpha", "PurlLookupResult", PurlLookupResult)


@dataclass(eq=False, repr=False)
class QueryContainerImagesRequest(betterproto2.Message):
    """
    QueryContainerImagesRequest contains an OCI Chain ID to search for.
    """

    chain_id: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    An OCI Chain ID referring to an ordered sequence of OCI layers.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryContainerImagesRequest", QueryContainerImagesRequest)


@dataclass(eq=False, repr=False)
class QueryContainerImagesResult(betterproto2.Message):
    """
    QueryContainerImagesResults contains repositories corresponding to the
    queried OCI Chain ID.
    """

    results: "list[QueryContainerImagesResultResult]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The query results. At most 1000 results are returned.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryContainerImagesResult", QueryContainerImagesResult)


@dataclass(eq=False, repr=False)
class QueryContainerImagesResultResult(betterproto2.Message):
    registry: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The URL of the upstream registry where the image was available from,
    even if fetched from a mirror.
    """

    repository: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    An image repository (eg. 'tensorflow') referring to collection of
    images.
    """

    docker_metadata: "DockerHubRepoMetadata | None" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The DockerHub-specific repository metadata. Only populated if this image
    is available on DockerHub.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "QueryContainerImagesResult.Result", QueryContainerImagesResultResult
)


@dataclass(eq=False, repr=False)
class QueryRequest(betterproto2.Message):
    """
    QueryRequest identifies package versions for which to return information.
    At least one of its fields must be set, and both fields may be set to narrow
    the results.
    """

    hash: "Hash | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    A content hash for an artifact associated with a package version, such as
    a JAR file. Currently supported for npm, Cargo, Maven, NuGet, PyPI, and
    RubyGems. Note that hashes and package versions have a many-to-many
    relationship.
    """

    version_key: "VersionKey | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The name of the package version.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryRequest", QueryRequest)


@dataclass(eq=False, repr=False)
class QueryResult(betterproto2.Message):
    """
    QueryResult holds information about package versions matching the query.
    """

    results: "list[QueryResultResult]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Package versions matching the query. At most 1000 versions are returned.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryResult", QueryResult)


@dataclass(eq=False, repr=False)
class QueryResultResult(betterproto2.Message):
    version: "Version | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    A package version matching the query.
    """

    artifacts: "list[QueryResultResultArtifact]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    If a hash was specified in the request, artifacts describes the upstream
    artifacts matching the hash.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryResult.Result", QueryResultResult)


@dataclass(eq=False, repr=False)
class QueryResultResultArtifact(betterproto2.Message):
    """
    An artifact is content associated with a package version, such as a JAR
    file. Currently supported for npm, Cargo, Maven, NuGet, PyPI and
    RubyGems.
    """

    url: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The origin of the artifact with this hash.
    """


default_message_pool.register_message("deps_dev.v3alpha", "QueryResult.Result.Artifact", QueryResultResultArtifact)


@dataclass(eq=False, repr=False)
class Requirements(betterproto2.Message):
    """
    Requirements contains a system-specific representation of the requirements
    specified by a package version. Only one of its fields will be set.
    """

    nuget: "RequirementsNuGet | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The NuGet-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single unresolved
    requirement" to be consistent with how the term is used in the NuGet
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """

    npm: "RequirementsNpm | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The npm-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single unresolved
    requirement" to be consistent with how the term is used in the npm
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """

    maven: "RequirementsMaven | None" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The Maven-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single unresolved
    requirement" to be consistent with how the term is used in the Maven
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.

    This data is as it is declared in a version POM file. The data in parent
    POMs are not merged.
    Any string field may contain references to properties, and the properties
    are not interpolated.
    """

    rubygems: "RequirementsRubyGems | None" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The RubyGems-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single unresolved
    requirement" to be consistent with how the term is used in the npm
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """

    go: "RequirementsGo | None" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The Go-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single
    requirement" to be consistent with how the term is used in the Go
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """

    pypi: "RequirementsPyPi | None" = betterproto2.field(7, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The PyPI-specific representation of the version's requirements.

    Note that the term "dependency" is used here to mean "a single
    requirement" to be consistent with how the term is used in the Python
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """

    cargo: "RequirementsCargo | None" = betterproto2.field(8, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The Cargo-specific representation of a crate version's requirements.

    Note that the term "dependency" is used here to mean "a single
    requirement" to be consistent with how the term is used in the Cargo
    ecosystem. This is different to how it is used elsewhere in the deps.dev
    API.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements", Requirements)


@dataclass(eq=False, repr=False)
class RequirementsCargo(betterproto2.Message):
    dependencies: "list[RequirementsCargoDependency]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)

    features: "list[RequirementsCargoFeature]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Cargo", RequirementsCargo)


@dataclass(eq=False, repr=False)
class RequirementsCargoDependency(betterproto2.Message):
    """
    Dependency represents a single dependency entry in a Cargo.toml file.
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    requirement: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)

    kind: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Whether this is a normal dependency, dev-dependency, or
    build-dependency.
    """

    optional: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    Whether this is an optional dependency.
    """

    package_alias: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    The name of the package as it is used in the source code.
    """

    uses_default_features: "bool" = betterproto2.field(6, betterproto2.TYPE_BOOL)
    """
    Whether the dependency uses its default features.
    """

    features: "list[str]" = betterproto2.field(7, betterproto2.TYPE_STRING, repeated=True)
    """
    Features that are enabled on this dependency.
    """

    target: "str" = betterproto2.field(8, betterproto2.TYPE_STRING)
    """
    If present, then this dependency is specific to a given platform.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Cargo.Dependency", RequirementsCargoDependency)


@dataclass(eq=False, repr=False)
class RequirementsCargoFeature(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    implies: "list[str]" = betterproto2.field(2, betterproto2.TYPE_STRING, repeated=True)
    """
    List of other features or dependencies that this feature would then
    enable.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Cargo.Feature", RequirementsCargoFeature)


@dataclass(eq=False, repr=False)
class RequirementsGo(betterproto2.Message):
    direct_dependencies: "list[RequirementsGoDependency]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)

    indirect_dependencies: "list[RequirementsGoDependency]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)

    replaces: "list[RequirementsGoReplace]" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Replace represents a replace directive in a go.mod file, which replaces
    the contents of a module with contents found elsewhere.
    See https://go.dev/ref/mod#go-mod-file-replace
    """

    excludes: "list[RequirementsGoDependency]" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Excludes represents exclude directives in a go.mod file, which specify
    versions of a module that are ignored.
    See https://go.dev/ref/mod#go-mod-file-exclude.

    Note: Before Go 1.16, excluded versions resulted in the next higher
    available version being silently chosen and not committed to go.mod,
    leading to non-deterministic builds. From Go 1.16 onwards, the
    substituting dependency must be explicitly captured in go.mod.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Go", RequirementsGo)


@dataclass(eq=False, repr=False)
class RequirementsGoDependency(betterproto2.Message):
    """
    Dependency represents a module requirement in a go.mod file.
    The requirement field contains the version query (e.g. "v1.2.3").
    See https://go.dev/ref/mod#go-mod-file-require
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    requirement: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Go.Dependency", RequirementsGoDependency)


@dataclass(eq=False, repr=False)
class RequirementsGoReplace(betterproto2.Message):
    src: "RequirementsGoDependency | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The module to be replaced. The version (in the requirement field) is
    optional. If omitted, *all* versions of the module are replaced.
    """

    replacement: "RequirementsGoDependency | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The replacement can either be another remote module (specifying name
    and version) or a path to a local module. Exactly one of replacement
    or local_path will be set.
    """

    local_path: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The path to a local directory containing the replacement module.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Go.Replace", RequirementsGoReplace)


@dataclass(eq=False, repr=False)
class RequirementsMaven(betterproto2.Message):
    parent: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The direct parent of a package version.
    """

    dependencies: "list[RequirementsMavenDependency]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The list of dependencies.
    """

    dependency_management: "list[RequirementsMavenDependency]" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The list of dependency management.
    The format is the same as dependencies.
    """

    properties: "list[RequirementsMavenProperty]" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The list of properties, used to resolve placeholders.
    """

    repositories: "list[RequirementsMavenRepository]" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The list of repositories.
    """

    profiles: "list[RequirementsMavenProfile]" = betterproto2.field(6, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The list of profiles.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Maven", RequirementsMaven)


@dataclass(eq=False, repr=False)
class RequirementsMavenDependency(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the package.
    """

    version: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The version requirement of the dependency.
    """

    classifier: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The classifier of the dependency, which distinguishes artifacts that
    differ in content.
    """

    type: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The type of the dependency, defaults to jar.
    """

    scope: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    The scope of the dependency, specifies how to limit the transitivity
    of a dependency.
    """

    optional: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    Whether the dependency is optional or not.
    """

    exclusions: "list[str]" = betterproto2.field(7, betterproto2.TYPE_STRING, repeated=True)
    """
    The dependencies to be excluded, in the form of a list of package
    names.
    Exclusions may contain wildcards in both groupID and artifactID.
    """

    resolved_version: "str" = betterproto2.field(8, betterproto2.TYPE_STRING)
    """
    The resolved instances of the above version/name fields. I.e. If the
    version field contained a Maven property, then the resolved version
    will have the actual property's value interpolated in.
    """

    resolved_name: "str" = betterproto2.field(9, betterproto2.TYPE_STRING)

    origin: "str" = betterproto2.field(10, betterproto2.TYPE_STRING)
    """
    The origin of the dependency. Can be any of the following values:
    "" - declared in the current package's merged, effective POM. I.e. the
         POM formed from merging all the ancestor POMs.
    "parent" - this dependency represents the parent package's POM.
    "management" - declared in a local or inherited `<dependencyManagement>`
    section. "import" - declared in another BOM POM in the
    `<dependencyManagement>` section.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Maven.Dependency", RequirementsMavenDependency)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfile(betterproto2.Message):
    id: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The ID of the profile.
    """

    activation: "RequirementsMavenProfileActivation | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The activation requirement of the profile.
    """

    dependencies: "list[RequirementsMavenDependency]" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The dependencies specified in the profile.
    """

    dependency_management: "list[RequirementsMavenDependency]" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The dependency management specified in the profile.
    """

    properties: "list[RequirementsMavenProperty]" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The properties specified in the profile.
    """

    repositories: "list[RequirementsMavenRepository]" = betterproto2.field(6, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The repositories specified in the profile.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Maven.Profile", RequirementsMavenProfile)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfileActivation(betterproto2.Message):
    active_by_default: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    Whether the profile is active by default.
    """

    jdk: "RequirementsMavenProfileActivationJdk | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The JDK requirement of the activation.
    """

    os: "RequirementsMavenProfileActivationOs | None" = betterproto2.field(3, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The operating system requirement of the activation.
    """

    property: "RequirementsMavenProfileActivationProperty | None" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, optional=True
    )
    """
    The property requirement of the activation.
    """

    file: "RequirementsMavenProfileActivationFile | None" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The file requirement of the activation.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.Maven.Profile.Activation", RequirementsMavenProfileActivation
)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfileActivationFile(betterproto2.Message):
    exists: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the file that its existence activates the profile.
    """

    missing: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The name of the file, activate the profile if the file is missing.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.Maven.Profile.Activation.File", RequirementsMavenProfileActivationFile
)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfileActivationJdk(betterproto2.Message):
    jdk: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The JDK requirement to activate the profile.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.Maven.Profile.Activation.JDK", RequirementsMavenProfileActivationJdk
)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfileActivationOs(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the operating system.
    """

    family: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The family of the operating system.
    """

    arch: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The CPU architecture of the operating system,
    """

    version: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The version of the operating system.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.Maven.Profile.Activation.OS", RequirementsMavenProfileActivationOs
)


@dataclass(eq=False, repr=False)
class RequirementsMavenProfileActivationProperty(betterproto2.Message):
    property: "RequirementsMavenProperty | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The property requirement to activate the profile.
    This can be a system property or CLI user property.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.Maven.Profile.Activation.Property", RequirementsMavenProfileActivationProperty
)


@dataclass(eq=False, repr=False)
class RequirementsMavenProperty(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the property.
    """

    value: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The value of the property.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Maven.Property", RequirementsMavenProperty)


@dataclass(eq=False, repr=False)
class RequirementsMavenRepository(betterproto2.Message):
    id: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The ID of the repository.
    """

    url: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The URL of the repository.
    """

    layout: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Whether the description of the repository follows a common layout.
    """

    releases_enabled: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    Whether the repository is enabled for release downloads.
    """

    snapshots_enabled: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    Whether the repository is enabled for snapshot downloads.
    """

    resolved_url: "str" = betterproto2.field(6, betterproto2.TYPE_STRING)
    """
    The resolved instance of URL. I.e. If the URL contained a Maven
    property, then the resolved URL will have the actual property's value
    interpolated in.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.Maven.Repository", RequirementsMavenRepository)


@dataclass(eq=False, repr=False)
class RequirementsNpm(betterproto2.Message):
    dependencies: "RequirementsNpmDependencies | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The dependency-related fields declared in the requested package version's
    package.json.
    """

    bundled: "list[RequirementsNpmBundle]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Contents of any additional package.json files found inside the
    "node_modules" folder of the version's tarball, including nested
    "node_modules".
    """

    os: "list[str]" = betterproto2.field(3, betterproto2.TYPE_STRING, repeated=True)
    """
    The operating systems that this package version can run on.
    """

    cpu: "list[str]" = betterproto2.field(4, betterproto2.TYPE_STRING, repeated=True)
    """
    The CPU architectures that this package version can run on.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.NPM", RequirementsNpm)


@dataclass(eq=False, repr=False)
class RequirementsNpmBundle(betterproto2.Message):
    path: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The path inside the tarball where this dependency was found.
    """

    name: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The name of the bundled package, as declared inside the bundled
    package.json.
    """

    version: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The version of this package, as declared inside the bundled
    package.json.
    """

    dependencies: "RequirementsNpmDependencies | None" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The dependency-related fields from the bundled package.json.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.NPM.Bundle", RequirementsNpmBundle)


@dataclass(eq=False, repr=False)
class RequirementsNpmDependencies(betterproto2.Message):
    dependencies: "list[RequirementsNpmDependenciesDependency]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The "dependencies" field of a package.json, represented as a list of
    name, requirement pairs.
    """

    dev_dependencies: "list[RequirementsNpmDependenciesDependency]" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The "devDependencies" field of a package.json. The format is the
    same as "dependencies".
    """

    optional_dependencies: "list[RequirementsNpmDependenciesDependency]" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The "optionalDependencies" field of a package.json. The format is
    the same as "dependencies".
    """

    peer_dependencies: "list[RequirementsNpmDependenciesDependency]" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The "peerDependencies" field of a package.json. The format is the
    same as "dependencies".
    """

    bundle_dependencies: "list[str]" = betterproto2.field(5, betterproto2.TYPE_STRING, repeated=True)
    """
    The "bundleDependencies" field of a package.json: a list of package
    names. In the package.json this may also just be the boolean value
    "true", in which case this field will contain the names of all the
    dependencies from the "dependencies" field.
    """

    peer_dependency_metadata: "list[RequirementsNpmDependenciesPeerDependencyMetadata]" = betterproto2.field(
        6, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The "peerDependencyMetadata" field of a package.json. A list of peer
    dependency names that may be marked optional.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.NPM.Dependencies", RequirementsNpmDependencies)


@dataclass(eq=False, repr=False)
class RequirementsNpmDependenciesDependency(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the package, the key in the original object.
    """

    requirement: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The requirement, the corresponding value from the original object.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.NPM.Dependencies.Dependency", RequirementsNpmDependenciesDependency
)


@dataclass(eq=False, repr=False)
class RequirementsNpmDependenciesPeerDependencyMetadata(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the package from the peerDependencies field.
    """

    optional: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    Whether the connection is marked optional inside
    peerDependenciesMeta.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha",
    "Requirements.NPM.Dependencies.PeerDependencyMetadata",
    RequirementsNpmDependenciesPeerDependencyMetadata,
)


@dataclass(eq=False, repr=False)
class RequirementsNuGet(betterproto2.Message):
    dependency_groups: "list[RequirementsNuGetDependencyGroup]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The requirements grouped by target framework.
    """

    target_frameworks: "list[str]" = betterproto2.field(2, betterproto2.TYPE_STRING, repeated=True)

    development_dependency: "bool" = betterproto2.field(3, betterproto2.TYPE_BOOL)

    framework_assemblies: "list[RequirementsNuGetFrameworkAssembly]" = betterproto2.field(
        4, betterproto2.TYPE_MESSAGE, repeated=True
    )

    framework_references: "list[RequirementsNuGetFrameworkReference]" = betterproto2.field(
        5, betterproto2.TYPE_MESSAGE, repeated=True
    )


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.NuGet", RequirementsNuGet)


@dataclass(eq=False, repr=False)
class RequirementsNuGetDependencyGroup(betterproto2.Message):
    target_framework: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The target framework that this dependency group is for.
    """

    dependencies: "list[RequirementsNuGetDependencyGroupDependency]" = betterproto2.field(
        2, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The requirements belonging to this dependency group.
    """


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.NuGet.DependencyGroup", RequirementsNuGetDependencyGroup
)


@dataclass(eq=False, repr=False)
class RequirementsNuGetDependencyGroupDependency(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the package.
    """

    requirement: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The requirement on the package.
    """

    include: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)

    exclude: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.NuGet.DependencyGroup.Dependency", RequirementsNuGetDependencyGroupDependency
)


@dataclass(eq=False, repr=False)
class RequirementsNuGetFrameworkAssembly(betterproto2.Message):
    assembly_name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    target_framework: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.NuGet.FrameworkAssembly", RequirementsNuGetFrameworkAssembly
)


@dataclass(eq=False, repr=False)
class RequirementsNuGetFrameworkReference(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    target_framework: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.NuGet.FrameworkReference", RequirementsNuGetFrameworkReference
)


@dataclass(eq=False, repr=False)
class RequirementsPyPi(betterproto2.Message):
    dependencies: "list[RequirementsPyPiDependency]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)

    provided_extras: "list[RequirementsPyPiExtra]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)

    external_dependencies: "list[RequirementsPyPiExternalDependency]" = betterproto2.field(
        3, betterproto2.TYPE_MESSAGE, repeated=True
    )

    required_python_version: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.PyPI", RequirementsPyPi)


@dataclass(eq=False, repr=False)
class RequirementsPyPiDependency(betterproto2.Message):
    project_name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    extras: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)

    version_specifier: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)

    environment_marker: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.PyPI.Dependency", RequirementsPyPiDependency)


@dataclass(eq=False, repr=False)
class RequirementsPyPiExternalDependency(betterproto2.Message):
    """
    ExternalDependency represents dependencies that can't be managed by the
    Python package manager but must be installed on the consumer's system.
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    version_specifier: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)

    environment_marker: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)


default_message_pool.register_message(
    "deps_dev.v3alpha", "Requirements.PyPI.ExternalDependency", RequirementsPyPiExternalDependency
)


@dataclass(eq=False, repr=False)
class RequirementsPyPiExtra(betterproto2.Message):
    """
    Extra represents Python's extra features/capabilities for this package.
    They are enabled in the items under the Requires-Dist.
    """

    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.PyPI.Extra", RequirementsPyPiExtra)


@dataclass(eq=False, repr=False)
class RequirementsRubyGems(betterproto2.Message):
    runtime_dependencies: "list[RequirementsRubyGemsDependency]" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, repeated=True
    )
    """
    The list of runtime dependencies.
    """

    dev_dependencies: "list[RequirementsRubyGemsDependency]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The list of development dependencies.
    """

    platform: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    Platform identifier for versions with platform specific code.
    """

    required_ruby_version: "str" = betterproto2.field(4, betterproto2.TYPE_STRING)
    """
    The required version of Ruby.
    """

    required_rubygems_version: "str" = betterproto2.field(5, betterproto2.TYPE_STRING)
    """
    The required version of RubyGems.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.RubyGems", RequirementsRubyGems)


@dataclass(eq=False, repr=False)
class RequirementsRubyGemsDependency(betterproto2.Message):
    name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The name of the package.
    """

    requirement: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The requirement on the package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Requirements.RubyGems.Dependency", RequirementsRubyGemsDependency)


@dataclass(eq=False, repr=False)
class SimilarlyNamedPackages(betterproto2.Message):
    """
    SimilarlyNamedPackages contains packages with similar names.
    """

    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The name of the package. Note that the package name may differ from the
    name in the request, due to canonicalization.
    """

    packages: "list[SimilarlyNamedPackagesPackage]" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Packages with names that deps.dev has calculated are similar to the name
    of the requested package. Note that this is not necessarily a symmetric
    relation, as we take into account popularity when calculating similar
    names.
    """


default_message_pool.register_message("deps_dev.v3alpha", "SimilarlyNamedPackages", SimilarlyNamedPackages)


@dataclass(eq=False, repr=False)
class SimilarlyNamedPackagesPackage(betterproto2.Message):
    package_key: "PackageKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
     A package with a name similar to the requested package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "SimilarlyNamedPackages.Package", SimilarlyNamedPackagesPackage)


@dataclass(eq=False, repr=False)
class SlsaProvenance(betterproto2.Message):
    """
    SLSAProvenance contains provenance information extracted from a SLSA
    provenance statement.
    """

    source_repository: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The source code repository used to build the version.
    """

    commit: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The commit of the source code repository the version was built from.
    """

    url: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The URL of the provenance statement if there is one.
    """

    verified: "bool" = betterproto2.field(4, betterproto2.TYPE_BOOL)
    """
    The Sigstore bundle containing this attestation was verified using the
    [sigstore-go](https://github.com/sigstore/sigstore-go) library.
    """


default_message_pool.register_message("deps_dev.v3alpha", "SLSAProvenance", SlsaProvenance)


@dataclass(eq=False, repr=False)
class UpstreamIdentifier(betterproto2.Message):
    """
    Non-normalized upstream identifiers encountered when syncing this package
    version.
    """

    package_name: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    The non-normalized package name string.
    """

    version_string: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The non-normalized version string.
    """

    source: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The upstream source for this identifier.
    """


default_message_pool.register_message("deps_dev.v3alpha", "UpstreamIdentifier", UpstreamIdentifier)


@dataclass(eq=False, repr=False)
class Version(betterproto2.Message):
    """
    Version holds information about a package version.
    """

    version_key: "VersionKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The name of the package version. Note that the package and version name
    may differ from names specified in requests, if applicable, due to
    canonicalization.
    """

    purl: "str" = betterproto2.field(9, betterproto2.TYPE_STRING)
    """
    The purl that identifies this package version.
    Note that the package and version name in the purl may differ from the
    names specified in the request, if applicable, due to canonicalization.
    """

    published_at: "datetime.datetime | None" = betterproto2.field(
        6, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )
    """
    The time when this package version was published, if available, as
    reported by the package management authority.
    """

    is_default: "bool" = betterproto2.field(2, betterproto2.TYPE_BOOL)
    """
    If true, this is the default version of the package: the version that is
    installed when no version is specified. The precise meaning of this is
    system-specific, but it is commonly the version with the greatest version
    number, ignoring pre-release versions.
    """

    is_deprecated: "bool" = betterproto2.field(12, betterproto2.TYPE_BOOL)
    """
    If true, this version has been marked as deprecated.
    """

    deprecated_reason: "str" = betterproto2.field(17, betterproto2.TYPE_STRING)
    """
    The reason why this version is deprecated.
    """

    licenses: "list[str]" = betterproto2.field(3, betterproto2.TYPE_STRING, repeated=True)
    """
    The licenses governing the use of this package version.

    We identify licenses as
    [SPDX 2.1](https://spdx.dev/spdx-specification-21-web-version/)
    expressions. When there is no associated SPDX identifier, we identify a
    license as "non-standard". When we are unable to obtain license
    information, this field is empty. When more than one license is listed,
    their relationship is unspecified.

    For Cargo, Maven, npm, NuGet, PyPI, and RubyGems, license information is
    read from the package metadata. For Go, license information is determined
    using the [licensecheck](https://github.com/google/licensecheck) package.

    License information is not intended to be legal advice, and you should
    independently verify the license or terms of any software for your own
    needs.
    """

    license_details: "list[VersionLicense]" = betterproto2.field(13, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Detailed information about the licenses governing the use of this package
    version.

    When deps.dev is unable to obtain license information, this field is
    empty.

    License information is not intended to be legal advice, and you should
    independently verify the license or terms of any software for your own
    needs.
    """

    advisory_keys: "list[AdvisoryKey]" = betterproto2.field(4, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Security advisories known to affect this package version directly. Further
    information can be requested using the Advisory method.

    Note that this field does not include advisories that affect dependencies
    of this package version.
    """

    links: "list[Link]" = betterproto2.field(5, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Links declared by or derived from package version metadata, to external
    web resources such as a homepage or source code repository. Note that
    these links are not verified for correctness.
    """

    slsa_provenances: "list[SlsaProvenance]" = betterproto2.field(7, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    SLSA provenance information for this package version. Extracted from a
    SLSA provenance attestation. This is only populated for npm package
    versions. See the 'attestations' field for more attestations (including
    SLSA provenance) for all systems.
    """

    attestations: "list[Attestation]" = betterproto2.field(14, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Attestations for this package version.
    """

    registries: "list[str]" = betterproto2.field(8, betterproto2.TYPE_STRING, repeated=True)
    """
    URLs for the package management registries this package version is
    available from.
    Only set for systems that use a central repository for package
    distribution: Cargo, Maven, npm, NuGet, PyPI, and RubyGems.
    """

    related_projects: "list[VersionProject]" = betterproto2.field(10, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Projects that are related to this package version.
    """

    upstream_identifiers: "list[UpstreamIdentifier]" = betterproto2.field(11, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    Some upstream identifiers used to refer to this package version.
    """

    cooldown: "VersionCooldown | None" = betterproto2.field(15, betterproto2.TYPE_MESSAGE, optional=True)
    """
    May be nil.
    """

    project_status: "VersionProjectStatus | None" = betterproto2.field(16, betterproto2.TYPE_MESSAGE, optional=True)
    """
    Whether a project is actively maintained, deprecated, etc. This field is
    not set for any package version outside of the PyPI system. See
    https://packaging.python.org/en/latest/specifications/project-status-markers/#project-status-markers
    for more information.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Version", Version)


@dataclass(eq=False, repr=False)
class VersionCooldown(betterproto2.Message):
    """
    A package cooldown is a security practice that mitigates supply chain
    attacks by delaying the installation of newly published package versions.
    This waiting period provides a critical time window for security
    researchers and automated tools to identify and flag potential malware or
    malicious code before the version is widely adopted.
    """

    end: "datetime.datetime | None" = betterproto2.field(
        1, betterproto2.TYPE_MESSAGE, unwrap=lambda: __google__protobuf__.Timestamp, optional=True
    )

    description: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    Optional.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Version.Cooldown", VersionCooldown)


@dataclass(eq=False, repr=False)
class VersionLicense(betterproto2.Message):
    license: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)
    """
    For Cargo, Maven, npm, NuGet, PyPI, and RubyGems, this field holds the
    license as specified by the package author in the package metadata. For
    Go, it holds license information as determined using the
    [licensecheck](https://github.com/google/licensecheck) package.
    """

    spdx: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The license mapped to an
    [SPDX 2.1](https://spdx.dev/spdx-specification-21-web-version/)
    expression, or "non-standard" if the license cannot be unambiguously
    mapped to SPDX.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Version.License", VersionLicense)


@dataclass(eq=False, repr=False)
class VersionProject(betterproto2.Message):
    project_key: "ProjectKey | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The identifier for the project.
    """

    relation_provenance: "ProjectRelationProvenance" = betterproto2.field(
        2, betterproto2.TYPE_ENUM, default_factory=lambda: ProjectRelationProvenance(0)
    )
    """
    How the mapping between project and package version was discovered.
    """

    relation_type: "ProjectRelationType" = betterproto2.field(
        3, betterproto2.TYPE_ENUM, default_factory=lambda: ProjectRelationType(0)
    )
    """
    What the relationship between the project and the package version is.
    """


default_message_pool.register_message("deps_dev.v3alpha", "Version.Project", VersionProject)


@dataclass(eq=False, repr=False)
class VersionProjectStatus(betterproto2.Message):
    status: "str" = betterproto2.field(1, betterproto2.TYPE_STRING)

    reason: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)


default_message_pool.register_message("deps_dev.v3alpha", "Version.ProjectStatus", VersionProjectStatus)


@dataclass(eq=False, repr=False)
class VersionBatch(betterproto2.Message):
    """
    VersionBatch contains a batch of version information. If there is more
    version information to be returned, a page token is included in the
    response.
    """

    responses: "list[VersionBatchResponse]" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, repeated=True)
    """
    The Version information for this page.
    """

    next_page_token: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    If set, this batch is not the full result set. This page token
    may be used to fetch more results in a subsequent request.
    """


default_message_pool.register_message("deps_dev.v3alpha", "VersionBatch", VersionBatch)


@dataclass(eq=False, repr=False)
class VersionBatchResponse(betterproto2.Message):
    request: "GetVersionRequest | None" = betterproto2.field(1, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The uncanonicalized request.
    """

    version: "Version | None" = betterproto2.field(2, betterproto2.TYPE_MESSAGE, optional=True)
    """
    The version information for the request. If the version was not found,
    this field is empty.
    """


default_message_pool.register_message("deps_dev.v3alpha", "VersionBatch.Response", VersionBatchResponse)


@dataclass(eq=False, repr=False)
class VersionKey(betterproto2.Message):
    """
    VersionKey identifies a package version by name.
    """

    system: "System" = betterproto2.field(1, betterproto2.TYPE_ENUM, default_factory=lambda: System(0))
    """
    The package management system containing the package.
    """

    name: "str" = betterproto2.field(2, betterproto2.TYPE_STRING)
    """
    The name of the package.
    """

    version: "str" = betterproto2.field(3, betterproto2.TYPE_STRING)
    """
    The version of the package.
    """


default_message_pool.register_message("deps_dev.v3alpha", "VersionKey", VersionKey)


class InsightsStub(betterproto2_grpclib.ServiceStub):
    """
    The Deps.dev Insights API provides information about open source software
    packages, projects, and security advisories. The information is gathered
    from upstream services like npm, GitHub, and OSV, and augmented by computing
    dependencies and relationships between entities.
    """

    async def get_package(
        self,
        message: "GetPackageRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Package":
        """
        GetPackage returns information about a package, including a list of its
        available versions, with the default version marked if known.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetPackage",
            message,
            Package,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_version(
        self,
        message: "GetVersionRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Version":
        """
        GetVersion returns information about a specific package version, including
        its licenses and any security advisories known to affect it.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetVersion",
            message,
            Version,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_version_batch(
        self,
        message: "GetVersionBatchRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "VersionBatch":
        """
        GetVersionBatch performs GetVersion requests for a batch of versions.
        Large result sets may be paginated.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetVersionBatch",
            message,
            VersionBatch,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_findings(
        self,
        message: "GetFindingsRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Findings":
        """
        GetFindings evaluates a specified package or version and returns
        findings which are relevant to safe dependency management.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetFindings",
            message,
            Findings,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_findings_batch(
        self,
        message: "GetFindingsBatchRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "FindingsBatch":
        """
        GetFindingsBatch performs GetFindings requests for a batch of
        packages/versions. Large result sets may be paginated.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetFindingsBatch",
            message,
            FindingsBatch,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_requirements(
        self,
        message: "GetRequirementsRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Requirements":
        """
        GetRequirements returns the requirements for a given version in a
        system-specific format. Requirements are currently available for
        Cargo, Go, Maven, npm, NuGet, PyPI, and RubyGems.

        Requirements are the dependency constraints specified by the version.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetRequirements",
            message,
            Requirements,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_dependencies(
        self,
        message: "GetDependenciesRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Dependencies":
        """
        GetDependencies returns a resolved dependency graph for the given package
        version. Dependencies are currently available for npm, Cargo, Maven
        and PyPI.

        Dependencies are the resolution of the requirements (dependency
        constraints) specified by a version.

        The dependency graph should be similar to one produced by installing the
        package version on a generic 64-bit Linux system, with no other
        dependencies present. The precise meaning of this varies from system to
        system.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetDependencies",
            message,
            Dependencies,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_dependents(
        self,
        message: "GetDependentsRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Dependents":
        """
        GetDependents returns information about the number of distinct packages
        known to depend on the given package version. Dependent counts are
        currently available for npm, Cargo, Maven and PyPI.

        Dependent counts are derived from the dependency graphs computed by
        deps.dev, which means that only public dependents are counted. As such,
        dependent counts should be treated as indicative of relative popularity
        rather than precisely accurate.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetDependents",
            message,
            Dependents,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_capabilities(
        self,
        message: "GetCapabilitiesRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Capabilities":
        """
        GetCapabilityRequest returns counts for direct and indirect calls to
        Capslock capabilities for a given package version.
        Currently only available for Go.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetCapabilities",
            message,
            Capabilities,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_project(
        self,
        message: "GetProjectRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Project":
        """
        GetProject returns information about projects hosted by GitHub, GitLab, or
        BitBucket, when known to us.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetProject",
            message,
            Project,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_project_batch(
        self,
        message: "GetProjectBatchRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "ProjectBatch":
        """
        GetProjectBatch performs GetProjectBatch requests for a batch of projects.
        Large result sets may be paginated.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetProjectBatch",
            message,
            ProjectBatch,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_project_package_versions(
        self,
        message: "GetProjectPackageVersionsRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "ProjectPackageVersions":
        """
        GetProjectPackageVersions returns known mappings between the requested
        project and package versions.
        At most 1500 package versions are returned. Mappings which were derived
        from attestations are served first.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetProjectPackageVersions",
            message,
            ProjectPackageVersions,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_advisory(
        self,
        message: "GetAdvisoryRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "Advisory":
        """
        GetAdvisory returns information about security advisories hosted by OSV.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetAdvisory",
            message,
            Advisory,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def get_similarly_named_packages(
        self,
        message: "GetSimilarlyNamedPackagesRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "SimilarlyNamedPackages":
        """
        GetSimilarlyNamedPackages returns packages with names that are similar to
        the requested package. This similarity relation is computed by deps.dev.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/GetSimilarlyNamedPackages",
            message,
            SimilarlyNamedPackages,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def query(
        self,
        message: "QueryRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "QueryResult":
        """
        Query returns information about multiple package versions, which can be
        specified by name, content hash, or both. If a hash was specified in the
        request, it returns the artifacts that matched the hash.

        Querying by content hash is currently supported for npm, Cargo, Maven,
        NuGet, PyPI, and RubyGems. It is typical for hash queries to return many
        results; hashes are matched against multiple release artifacts (such as
        JAR files) that comprise package versions, and any given artifact may
        appear in several package versions.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/Query",
            message,
            QueryResult,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def purl_lookup(
        self,
        message: "PurlLookupRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "PurlLookupResult":
        """
        PurlLookup searches for a package or package version specified via
        [purl](https://github.com/package-url/purl-spec),
        and returns the corresponding result from GetPackage or GetVersion as
        appropriate.

        For a package lookup, the purl should be in the form
          `pkg:type/namespace/name`   for a namespaced package name, or
          `pkg:type/name`             for a non-namespaced package name.

        For a package version lookup, the purl should be in the form
          `pkg:type/namespace/name@version`, or
          `pkg:type/name@version`.

        Extra fields in the purl must be empty, otherwise the request will fail.
        In particular, there must be no subpath or qualifiers.

        Supported values for `type` are `cargo`, `gem`, `golang`, `maven`, `npm`,
        `nuget`, and `pypi`. Further details on types, and how to form purls of
        each type, can be found in the [purl
        spec](https://github.com/package-url/purl-spec/blob/master/PURL-TYPES.rst).

        Special characters in purls must be percent-encoded. This is described in
        detail by the
        [purl
        spec](https://github.com/package-url/purl-spec/blob/master/PURL-SPECIFICATION.rst).
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/PurlLookup",
            message,
            PurlLookupResult,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def purl_lookup_batch(
        self,
        message: "PurlLookupBatchRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "PurlLookupBatchResult":
        """
        PurlLookupBatch performs PurlLookup requests for a batch of purls.
        This endpoint only supports version lookups. Purls in requests
        must include a version field.

        Supported purl forms are
          `pkg:type/namespace/name@version` for a namespaced package name, or
          `pkg:type/name@version`           for a non-namespaced package name.

        Extra fields in the purl must be empty, otherwise the request will fail.
        In particular, there must be no subpath or qualifiers.

        Large result sets may be paginated.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/PurlLookupBatch",
            message,
            PurlLookupBatchResult,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def query_container_images(
        self,
        message: "QueryContainerImagesRequest",
        *,
        timeout: "float | None" = None,
        deadline: "Deadline | None" = None,
        metadata: "MetadataLike | None" = None,
    ) -> "QueryContainerImagesResult":
        """
        QueryContainerImages searches for container image repositories on
        DockerHub that match the requested OCI Chain ID. At most 1000 image
        repositories are returned.

        An image repository is identifier (eg. 'tensorflow') that refers to
        a collection of images.

        An OCI Chain ID is a hashed encoding of an ordered sequence of OCI layers.
        For further details see the [OCI Chain ID
        spec](https://github.com/opencontainers/image-spec/blob/main/config.md#layer-chainid).
        If an image contains empty layers, it is available from this endpoint
        under two different chain IDs - one computed by including empty layers in
        the calculation and one computed by excluding them.
        """

        return await self._unary_unary(
            "/deps_dev.v3alpha.Insights/QueryContainerImages",
            message,
            QueryContainerImagesResult,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )


from ...google import protobuf as __google__protobuf__
